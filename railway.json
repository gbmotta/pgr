{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && python3 -m uvicorn api_sqlalchemy:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}

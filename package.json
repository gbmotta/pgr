{
  "name": "pgr-backend",
  "version": "3.0.0",
  "description": "Sistema PGR - Backend com build de frontend",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "build": "cd frontend-react && npm install && npm run build",
    "postinstall": "cd frontend-react && npm install && npm run build"
  }
}

